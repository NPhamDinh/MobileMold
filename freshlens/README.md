# FreshLens (Mobile App)

Flutter-based mobile application for capturing and analyzing images of food using smartphone microscopes for mold. iOS and Android supported. Requires a running backend that hosts the ML model (currently offline, as of January 2026).

## Setup
```bash
cd freshlens
flutter pub get
flutter run
